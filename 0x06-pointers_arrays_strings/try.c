#include "main.h"
#include <stdio.h>
/**
 * _atoi - converts a string to an integer
 * @s: string to convert
 *
 * Return: value of integer
 */
int _atoi(char *s)
{
	int ascii;
	char c = 'a'

        ascii = (int)c;
	printf("Ascii of %c is %d", c,ascii);
	return (0);
}
