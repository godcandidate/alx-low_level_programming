#include <stdlib.h>
#include <time.h>
/* more headers goes there */
#include <stdio.h>

/**
 * main - Prints the alphabets in lowercase
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)

{
	int i, k = '0', j;

	for (i = '0'; i <= '9'; i++)
	{
		for (j = k; j <= '9' j++)
		{
			if (i != z)
			{
				putchar(n);
				pytchar(z);
			}
			if ( i == j )
				break;
			else
			{
				putchar(',')
				putchar(' ')
			}
		}
		y++
        }
	putchar('\n');
	return (0);

}
