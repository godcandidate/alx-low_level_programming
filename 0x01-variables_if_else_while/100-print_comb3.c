#include <stdlib.h>
#include <time.h>
/* more headers goes there */
#include <stdio.h>

/**
 * main - Prints the alphabets in lowercase
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)

{
	int x, y = '0', z;

	for (x = '0'; x <= '9'; x++)
	{
		for (z = y; z <= '9' z++)
		{
			if ( n != z)
			{
				putchar(n);
				pytchar(z);
			if ( i == z )
				break;
			else
			{
				putchar(',')
				putchar(' ')
			}
		}
		y++
        }
	putchar('\n');
	return (0);

}
