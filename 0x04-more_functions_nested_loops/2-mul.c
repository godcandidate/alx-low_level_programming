#include "main.h"
/**
 *  mul - multiply two integers
 *  @a: first operand
 *  @b: second operand
 *
 *  Return: the results of the multiplication
 */
int mul(int a, int b)
{
	int results;

	results = a * b;
	return (results);
}
